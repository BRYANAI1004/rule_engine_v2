# MCG ingestion (placeholder)

This folder will hold **Step 2+** logic for parsing authorized MCG HTML snapshots into canonical **source tree** JSON (`types.ts` sketches the intended shape).

Step 0 does not implement parsing. Keep raw HTML under `rules/mcg/raw-html/` locally; do not commit licensed snapshots.
